import { View, Text } from 'react-native'
import React from 'react'

const PopularJobsCard = ({item}) => {
  return (
    <View>
      <Text>PopularJobsCard: {item}</Text>
    </View>
  )
}

export default PopularJobsCard
